import React, { Component } from 'react';
import { connect } from 'react-redux';
import './style.css';
import MainLayout from '../../layouts/main';

const ContainerStatsPage = ({  }) => (
    <MainLayout>
        404
    </MainLayout>
);
export default connect()(ContainerStatsPage);
